spring:
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
    gateway:
      routes: []

eureka:
  client:
    enabled: false

management:
  tracing:
    enabled: false
